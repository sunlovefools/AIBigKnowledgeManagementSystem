name: embedding-service
runtime: python3
entrypoint: uvicorn app:app --host 0.0.0.0 --port 8001
visibility: public
build:
  dockerfile: Dockerfile
resources:
  concurrency: 1
  accelerator: null   # CPU by default; use "nvidia-tesla-t4" if available
